/**
 * Fetches aggregated summary/analytics data.
 *  replace with real API calls.
 */

export async function fetchSummary(period = "3M") {
  // const res = await fetch(`/api/summary?period=${period}`);
  // return res.json();
  return Promise.resolve(null); 
}